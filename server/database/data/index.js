const client = require('./client');
const admin = require('./admin');
const exchangeMoney = require('./exchangeMoney');
const transactions = require('./transactions');

module.exports = { client, admin, exchangeMoney, transactions };
